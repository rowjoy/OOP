import 'super_class.dart';

class PlayGround extends Circle {
  @override
  double totalCircleArea(double side1, double side2) {
    return super.totalCircleArea(side1, side2);
  }
}

class FullPlayGround extends Circle {
  @override
  double totalCircleArea(double side1, double side2) {
    return super.totalCircleArea(side1, side2);
  }
}